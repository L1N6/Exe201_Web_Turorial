<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - EXE201_Tutor_Web</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/EXE201_Tutor_Web.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Paytone+One:wght@400&display=swap" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Paytone+One:wght@400&display=swap" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f2f2f2;
        }

        .chat-popup {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background-color: #614DE1;
            color: white;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
            z-index: 1000;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s ease-in-out;
        }

            .chat-popup:hover {
                background-color: #4b38aa;
            }

        .chat-box {
            display: none;
            position: fixed;
            bottom: 80px;
            right: 20px;
            width: 300px;
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1000;
            overflow: hidden;
            transform-origin: bottom right;
            transform: scaleY(0);
            transition: transform 0.3s ease-in-out;
        }

        .chat-popup:hover .chat-box {
            transform: scaleY(1);
        }

        .close-btn {
            cursor: pointer;
            float: right;
            padding: 10px;
            font-size: 18px;
            color: #614DE1;
            transition: color 0.3s ease-in-out;
        }

            .close-btn:hover {
                color: #4b38aa;
            }

        .chat-content {
            padding: 10px;
            max-height: 200px;
            overflow-y: auto;
        }

        .input-box {
            padding: 10px;
            display: flex;
        }

            .input-box input {
                flex: 1;
                padding: 8px;
                margin-right: 5px;
                border: 1px solid #ccc;
                border-radius: 5px;
                outline: none;
            }

            .input-box button {
                background-color: #614DE1;
                color: white;
                border: none;
                padding: 8px 12px;
                cursor: pointer;
                border-radius: 5px;
                transition: background-color 0.3s ease-in-out;
            }

                .input-box button:hover {
                    background-color: #4b38aa;
                }

        a[b-426q4sx0ff] {
            color: #614DE1 !important;
        }

        a, a:hover {
            text-decoration: none;
        }

        .banner-container {
            width: 100%;
            overflow: hidden;
            position: relative;
            height: 227px; /* Adjust height according to your design */
        }

        .banner {
            display: none;
            width: 100%;
        }

            .banner img {
                width: 100%;
            }

        .active {
            display: block;
        }

        .render-body-content {
            margin-top: calc(400 * var(--rpx));
            height: 400px;
        }

        .rectangle-6 {
            position: relative;
            width: calc(1440 * var(--rpx));
            height: calc(59 * var(--rpx));
            margin: 0 0 0 0;
            background: #aec3fa;
            z-index: 4;
            overflow: hidden; /* Hide overflow to crop images */
        }

        span {
            margin-top: 5px;
        }

    </style>
</head>
<body>
    <div class="main-container">
        <div class="rectangle">
            <a href="@Url.Action("Index", "Home")" class="rectangle-1">
                <img class="logo-image" src="../img/header/logo.png">
            </a>
            <span class="text-demo">DEMO </span>
            <a href="@Url.Action("Index", "Home")" class="bombastic-learning-hub">
                BOMBASTIC <br />LEARNING HUB
            </a>
            <a href="@Url.Action("SignIn", "Access")" class="rectangle-2">
                <span class="dang-nhap">Đăng nhập </span>
            </a>
            <a href="@Url.Action("SignUp", "Access")" class="rectangle-3">
                <span class="dang-ky">Đăng ký</span>
            </a>
            <div class="rectangle-4"><img style="border-radius:50px" src="../img/header/avatar.jpg" alt=""></div>
            <div class="rectangle-5"><img src="../img/header/login.png" alt=""></div>
        </div>
        <div class="rectangle-6" ">
            <a href="#" class="hotline">HOTLINE: 000-XXX-XXX</a>
            <a href="@Url.Action("Index", "Home")" class="trang-chu">Trang chủ</a>
            <a href="@Url.Action("Detail", "Navigation")" class="thong-tin-chi-tiet">Thông tin chi tiết</a>
            <a href="@Url.Action("Student", "Navigation")" class="hoc-vien">Học viên</a>
            <a href="@Url.Action("Job", "Navigation")" class="cong-viec">Công việc</a>
        </div>
        @RenderBody()
    </div>
    <footer>
        <script src="https://static.elfsight.com/platform/platform.js" data-use-service-core defer></script>
        <div class="elfsight-app-0f31743a-7b5d-4b21-a942-0d042151e62f" data-elfsight-app-lazy></div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
            @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
<script>

    document.addEventListener("DOMContentLoaded", function () {
        let currentSlide = 0;
        const slides = document.querySelectorAll(".banner");
        const totalSlides = slides.length;

        function showSlide(n) {
            slides.forEach(slide => {
                slide.style.display = "none";
            });
            slides[n].style.display = "block";
        }

        function nextSlide() {
            currentSlide = (currentSlide + 1) % totalSlides;
            showSlide(currentSlide);
        }

        function prevSlide() {
            currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
            showSlide(currentSlide);
        }

        const prevButton = document.createElement("button");
        prevButton.textContent = "Previous";
        prevButton.addEventListener("click", prevSlide);

        const nextButton = document.createElement("button");
        nextButton.textContent = "Next";
        nextButton.addEventListener("click", nextSlide);

        const controls = document.createElement("div");
        controls.classList.add("banner-controls");
        controls.appendChild(prevButton);
        controls.appendChild(nextButton);

        document.querySelector(".banner-container").appendChild(controls);

        showSlide(currentSlide);
    });
    // Lưu trạng thái của trang
    window.addEventListener('scroll', function () {
        sessionStorage.setItem('scrollPosition', window.scrollY);
    });

    // Phục hồi trạng thái của trang
    window.addEventListener('DOMContentLoaded', function () {
        const scrollPosition = sessionStorage.getItem('scrollPosition');
        if (scrollPosition) {
            window.scrollTo(0, scrollPosition);
        }
    });
</script>
